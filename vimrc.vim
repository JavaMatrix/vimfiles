" Color options
syntax on
colorscheme serengeti

" GUI options
set go      =icP              " Use an [i]con, try to use [c]onsole prompts, and allow copy/[P]aste with windows.  
set guifont =inconsolata:h12  " Use a nice monospace font at 12 points.

" Backspace like a normal human being
set backspace =indent,eol,start

" Start fullscreen
au GUIEnter * simalt ~x
